<h1>{{ 'reset_password.email.Hi!'|trans({}, 'interface') }}</h1>

<p>{{ 'reset_password.email.To reset your password, please visit the following link'|trans({}, 'interface') }}</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>
    {{ "reset_password.expire_link"|trans({}, 'interface') }} {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
</p>

<p>{{ 'reset_password.email.Cheers!'|trans({}, 'interface') }}</p>
