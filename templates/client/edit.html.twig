{% extends 'base.html.twig' %}

{% block title %}{{ "client.edit.title"|trans({'%client_name%': client.name}, 'interface') }}{% endblock %}
{% block tab_title %}{{ "client.edit.title"|trans({'%client_name%': client.name}, 'interface') }}{% endblock %}

{% block body %}
    {{ include('client/_delete_form.html.twig') }}
    <div class="card bg-white shadow border-1 p-3">
        {{ include('client/_form.html.twig') }}
    </div>
    <a class="btn btn-link float-end" href="{{ path('app_client_index') }}">
        <i class="fas fa-arrow-left"></i>
        {{ "client.edit.back_to_list"|trans({}, 'interface') }}
    </a>
{% endblock %}
